overwrite: true
schema: "./modules/**/*.gql"
documents: "./graphql/**/*.gql"
generates:
  ./graphql/graphql.ts: # Backend Resolvers
    plugins:
      - typescript
      - typescript-resolvers
      - typescript-operations
      - typescript-react-apollo
  ./graphql.schema.json: # Changes Reference file
    plugins:
      - introspection
